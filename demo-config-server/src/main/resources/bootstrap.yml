spring:
  application:
    name: springcloud-config-server
  profiles:
    active: remote

server:
  port: 9005
eureka:
  client:
    service-url:
      defaultZone: http://server:7000/eureka/, http://server2:7000/eureka/